{
    "rules": {
        ".read": true,
        ".write": true,
        "users": {
            ".indexOn": ["username"]
        },
        "rooms": {
            ".indexOn": ["status"]
        },
        "invites": {
             ".indexOn": ["toUserId", "sentAt"]
        },
        "friendRequests": {
            ".indexOn": ["fromUserId", "toUserId"]
        }
    }
}